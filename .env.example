# Server-only Tushare Pro credential. Configure it in Vercel; never expose it to Vite.
TUSHARE_TOKEN=

# Exact production browser origin allowed to read the public API cross-origin.
PUBLIC_APP_ORIGIN=
